
file(GLOB SHADERS
	"${CMAKE_CURRENT_SOURCE_DIR}/SFX/*.sfx"
)

file(GLOB SHADER_INCLUDES
	"${CMAKE_CURRENT_SOURCE_DIR}/SL/*.sl"
)
if(NOT "${SHADERS}" STREQUAL "")
add_multiplatform_sfx_shader_project( CoreShaders INCLUDES "${CMAKE_CURRENT_SOURCE_DIR}/Sfx"
								"${CMAKE_CURRENT_SOURCE_DIR}/SL" "${SIMUL_PLATFORM_DIR}/.."
							SOURCES ${SHADERS} ${SHADER_INCLUDES} OPTIONS -w DEFINES VULKAN_SDK=${VULKAN_SDK_DIR}
					 		OUTPUT "${CMAKE_BINARY_DIR}/shaderbin/$PLATFORM_NAME" FOLDER ${SIMUL_PLATFORM_FOLDER_PREFIX}/Shaders)
endif()